# Properties file with JDBC and JPA settings.
#
# Applied by <context:property-placeholder location="jdbc.properties"/> from
# various application context XML files (e.g., "applicationContext-*.xml").
# Targeted at system administrators, to avoid touching the context XML files.

# Properties that control the population of schema and data for a new data source
jdbc.initLocation=classpath:db/${db.script}/schema.sql
jdbc.dataLocation=classpath:db/${db.script}/data.sql

# 앱 시작 시 자동 DB 초기화(schema+data 실행) 여부. 런타임 -Djdbc.initialize 로 덮어쓰기 가능(OVERRIDE).
# 로컬 H2(인메모리)=true. AWS RDS는 최초 1회 초기화 후 false 권장(2대 동시 기동/재기동 시 data.sql 중복 INSERT 실패 방지).
jdbc.initialize=true

jpa.showSql=true

jdbc.driverClassName=${jdbc.driverClassName}
jdbc.url=${jdbc.url}
jdbc.username=${jdbc.username}
jdbc.password=${jdbc.password}

# Property that determines which database to use with an AbstractJpaVendorAdapter
jpa.database=${jpa.database}
